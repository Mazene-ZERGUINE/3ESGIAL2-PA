import { DateToNowPipe } from './date-to-now.pipe';

describe('DateToNowPipe', () => {
  it('create an instance', () => {
    const pipe = new DateToNowPipe();
    expect(pipe).toBeTruthy();
  });
});
